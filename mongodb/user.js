/**
 * Schema  表  定义数据的类型并由表Schema生成Model，导出Model
 */

//  var mongoose = require('mongoose');
//  var Schema = mongoose.Schema;
//  var UserSchema = new Schema({
//      userName: { type: String },
//      userpwd: { type: String },
//      userage: { type: Number },
//      logindate: { type: Date }
//  })

//  module.exports = mongoose.model('User', UserSchema)           //由schema生成Model模型,这里的User就是一个Model,通过User可以很方便的操作数据库
